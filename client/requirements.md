## Packages
recharts | For visualizing financial reports (income vs expenses)
framer-motion | For smooth page transitions and micro-interactions

## Notes
The application requires RTL (Right-to-Left) layout for Kurdish language support.
Authentication uses Replit Auth (OpenID Connect).
